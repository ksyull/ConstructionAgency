<Window x:Class="ConstructionAgency.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="260" Width="380" WindowStartupLocation="CenterScreen"
        Background="LightPink">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Логин (имя или email)"/>
        <TextBox   Grid.Row="1" x:Name="LoginBox" Margin="0,6,0,12"/>

        <TextBlock   Grid.Row="2" Text="Пароль"/>
        <PasswordBox Grid.Row="3" x:Name="PwdBox" Margin="0,6,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Войти" Width="100" Click="Login_Click" Margin="0,0,8,0"/>
            <Button Content="Регистрация" Width="120" Click="OpenRegister_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="5" x:Name="ErrorText" Foreground="Crimson" TextWrapping="Wrap"/>
    </Grid>
</Window>