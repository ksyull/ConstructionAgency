<Window x:Class="ConstructionAgency.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Админ-панель" Height="540" Width="880" WindowStartupLocation="CenterScreen"
        Background="#FFE4E9">
    <Grid Margin="12">
        <TabControl>
            <!-- Пользователи -->
            <TabItem Header="Пользователи">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
                        <Button Content="Обновить" Width="110" Margin="0,0,8,0" Click="RefreshUsers_Click"/>
                        <Button Content="Удалить выбранного" Width="160" Click="DeleteUser_Click"/>
                        <TextBlock x:Name="UsersInfo" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <DataGrid x:Name="UsersGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="70"/>
                            <DataGridTextColumn Header="UserName" Binding="{Binding UserName}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                            <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="110"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>

            <!-- Проекты -->
            <TabItem Header="Проекты">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                        <TextBlock Text="Добавить проект" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Center"/>
                            <TextBox   Grid.Row="0" Grid.Column="1" x:Name="ProjectTitleBox" Margin="6,0,8,0"/>

                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Категория:" VerticalAlignment="Center"/>
                            <ComboBox  Grid.Row="0" Grid.Column="3" x:Name="ProjectCategoryBox"
                                       DisplayMemberPath="Name" SelectedValuePath="CategoryId" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Описание:" VerticalAlignment="Top" Margin="0,6,0,0"/>
                            <TextBox   Grid.Row="1" Grid.Column="1" x:Name="ProjectDescriptionBox" Margin="6,6,8,0"
                                       AcceptsReturn="True" Height="60" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="1" Grid.Column="2" Text="Статус:" VerticalAlignment="Center" Margin="0,6,0,0"/>
                            <ComboBox  Grid.Row="1" Grid.Column="3" x:Name="ProjectStatusBox" Margin="0,6,0,0">
                                <ComboBoxItem Content="Draft"/>
                                <ComboBoxItem Content="Open"/>
                                <ComboBoxItem Content="InProgress"/>
                                <ComboBoxItem Content="Completed"/>
                            </ComboBox>
                        </Grid>

                        <StackPanel Orientation="Horizontal">
                            <Button Content="Добавить" Width="110" Click="AddProject_Click"/>
                            <Button Content="Удалить выбранный" Width="160" Margin="8,0,0,0" Click="DeleteProject_Click"/>
                            <TextBlock x:Name="ProjectMsg" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>

                    <DataGrid x:Name="ProjectsGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ProjectId}" Width="60"/>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="CategoryId" Binding="{Binding CategoryId}" Width="100"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                            <DataGridTextColumn Header="CreatedAt" Binding="{Binding CreatedAt}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>

            <!-- Фотографии проектов -->
            <TabItem Header="Фотографии проектов">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                        <TextBlock Text="Управление фотографиями проектов" FontWeight="Bold" Margin="0,0,0,6"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Проект:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox Grid.Column="1" x:Name="PhotosProjectComboBox" 
                                     SelectionChanged="PhotosProjectComboBox_SelectionChanged"/>
                            <Button Grid.Column="2" Content="Добавить фото" Width="120" Margin="8,0,0,0"
                                   Click="AddPhoto_Click"/>
                        </Grid>

                        <TextBlock x:Name="PhotosInfo" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="PhotosItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="LightGray" BorderThickness="1" Margin="0,0,8,8" 
                                           Background="White" Padding="4">
                                        <StackPanel Width="200">
                                            <Image Source="{Binding FilePath}" Height="120" Stretch="Uniform"/>
                                            <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" 
                                                      Margin="0,4,0,2" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding ProjectTitle}" FontSize="10" Foreground="DarkBlue"/>
                                            <TextBlock Text="{Binding UploadDate, StringFormat='dd.MM.yyyy HH:mm'}" 
                                                      Foreground="Gray" FontSize="9"/>
                                            <Button Content="Удалить" Margin="0,4,0,0" 
                                                   Click="DeletePhoto_Click"
                                                   Background="#FFFFE0E0" BorderBrush="LightCoral"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>